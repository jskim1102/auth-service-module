[pytest]
pythonpath = .
asyncio_mode = auto
# Pin async fixtures (incl. the autouse _clean_db) to the per-test event loop so
# the central cleanup can't interleave with a neighbouring test's connection
# (removes the loop-scope ambiguity that caused a rare cross-test flake).
asyncio_default_fixture_loop_scope = function
testpaths = tests
